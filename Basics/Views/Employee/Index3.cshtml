@model List<Employee>

<div class="display-3">
    Index3 deyiz.
</div>

@foreach (var employee in Model)
{
    <div class="lead my-2">
        <h3>@employee.FullName</h3>
        <p>
            @employee.Id
        </p>
        <p><strong>Age:</strong>@employee.Age</p>
        <hr/>
    </div>
}